name: contact_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_contacts: ^1.1.9+2
  url_launcher: ^6.2.5
  path_provider: ^2.1.1
  intl: ^0.18.1
  file_picker: ^6.1.12
  share_plus: ^11.0.0
  cross_file: ^0.3.4+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
